001.cpp (6ms):

It is natural to group four elements together to perform rotation, so the memory complexity can be O(1). The tricky part is the upper bound of the loop variable in the two-layer for loop. One may carelessly conduct multiple rotations or no rotations.
