001.cpp (4ms):

The problem is not difficult. Just solve it recursively. The trees are the same when their roots are the same, their left subtrees are the same and their right subtrees are the same. Pay attention to the empty tree. The time complexity is O(n).

