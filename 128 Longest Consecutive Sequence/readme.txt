001.cpp (12ms):

Perform the quick sort first. And linearly check the whole array. The time complexity is O(nlog(n)).

002.cpp (20ms):

Use the hash table to obtain the O(n) time complexity. The running time seems longer, because quick sort and linear checking can make good use of cache.
