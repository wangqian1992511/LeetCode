001.cpp (12ms):

Sort and then check the h-index. Time complexity is O(nlog(n)).

002.cpp (8ms):

Perform binary search to the final answer. Time complexity is O(nlog(n)).

003.cpp (8ms):

Use counting sort instead of quick sort. Time complexity is O(n).