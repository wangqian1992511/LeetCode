001.cpp (15ms):

Two pointer are pointing to two alphanumeric character at the head and the tail of the string. Repeat comparing the characters pointed by these pointers until the pointers meet together or we find two different characters being pointed to. The time complexity is O(n).

