001.cpp (28ms):

Use a queue to record the inorder traversal of a BST. Time complexity is O(n), memory complexity is O(n).

002.cpp (28ms):

Or it's also fine to use a stack to record the leftmost unpopped node. Time complexity is O(n), memory complexity is O(log(n)).