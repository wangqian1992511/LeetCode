001.cpp (0ms):

Use two queues. When a new element comes, move all the elements to "qb", so that this element will be pushed to an empty "qa". Then all the elements in "qb" can be moved to "qa" again.