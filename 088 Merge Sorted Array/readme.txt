001.cpp (7ms):

The time complexity to merge two sorted arrays is O(n), but more running time is needed than that for sorted linked list. Three pointers are used to maintain the two input arrays and the single output array.
