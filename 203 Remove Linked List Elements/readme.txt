001.cpp (40ms):

A tip for this kind of linked list based problem is to use the "super node". If we add a super node before the head of the original linked list, we can avoid one corner case. Thus, the programming complexity is reduced.
