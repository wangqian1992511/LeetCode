001.cpp (16ms):

A BST will be valid only when both its sub-trees are valid, the maximum value in the left subtree is less than the root and the minimum value in the right subtree is larger than the root.